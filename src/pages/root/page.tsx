import {MainContent} from './main-content/main-content';
import {Header} from './header/header';

export const Root = () => {
  return (
    <div>
      <Header/>
      <MainContent/>
    </div>
  );
};
